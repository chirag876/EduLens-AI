Steps to add new Source_Type:

Step 1 — static/enums.py
         SourceType mein naya type add karo
         e.g. SourceType.DOCUMENT = 'document'

Step 2 — static/collections.py
         Naya collection name add karo
         e.g. DOCUMENT_CHUNKS = 'document_chunks'

Step 3 — ingestion/new_source_ingestion.py  ← NEW FILE
         Fetch → Extract → Clean → Chunk → Embed → Store
         (pdf_ingestion.py ko reference banao)

Step 4 — models/ingestion_model.py
         Naya Request model add karo
         e.g. DocumentIngestionRequest

Step 5 — routes/ingestion.py
         Naya POST + DELETE endpoint add karo

Step 6 — retrieval/retriever.py
         Naya collection search block add karo

Step 7 — main.py
         Kuch nahi karna — routes already registered hain


----------------------------------------------------------------------------------------------------------------------------------------
Files that will not need to be changed (common components):

* `embeddings/embedder.py`
* `processing/cleaner.py` (only a new cleaning function may be added)
* `processing/chunker.py`
* `llm/generator.py`
* `safety/moderator.py`
* `personalization/personalizer.py`
